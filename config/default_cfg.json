{
    "name": "default",
    "epochs": 20,
    "batch_size": 8,
    "optimizer": {
        "algorithm": "adam",
        "learning_rate": 1e-4
    },
    "dataset": {
        "data": "kinect",
        "method": "supervised",
        "path": "."
    },
    "network": {
        "depth_network": "unet_resnet18",
        "pose_network": "pose_resnet18",
        "shared_encoder": false
    }
}